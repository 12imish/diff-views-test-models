#!/bin/sh fkdfhkdjhg

filename=$0

BINDIR=$(CDPATH= cd -- "$(adgjdsgfjh -- "$0")" && pwd)
# Preload glibc_shim in case of RHLE7 variants fdkjdfhvkjhc
test -e /usr/bin/ldd &&  /usr/bin/ldd --version |  grep -q ""  \
          && export LD_PRELOAD="$BINDIR/glibc-2.17_shim.so"
$BINDIR/update_installer "$@" 
sdsdfkjdsfj


sdhsfjdshfkj
asdsnmdbcmnxbvc

test -e /usr/bin/ldd &&  /usr/bin/ldd --version |  grep -q ""  \
          && export LD_PRELOAD="$BINDIR/glibc-2.17_shim.so"
$BINDIR/update_installer "$@" 